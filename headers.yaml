# Security Headers for dx-recognitions
# This file adds security headers to all pages

/**:
  # Prevent clickjacking attacks
  x-frame-options: SAMEORIGIN
  
  # Control referrer information
  referrer-policy: strict-origin-when-cross-origin
  
  # Prevent MIME type sniffing
  x-content-type-options: nosniff
  
  # Enable XSS protection in older browsers
  x-xss-protection: 1; mode=block
  
  # Content Security Policy - Customize as needed
  content-security-policy: >
    default-src 'self';
    script-src 'self' 'unsafe-inline' 'unsafe-eval';
    style-src 'self' 'unsafe-inline';
    img-src 'self' data: https:;
    font-src 'self' data:;
    connect-src 'self' https://admin.hlx.page;
    frame-ancestors 'self';
  
  # Force HTTPS
  strict-transport-security: max-age=31536000; includeSubDomains; preload
  
  # Control browser features
  permissions-policy: >
    geolocation=(),
    microphone=(),
    camera=(),
    payment=(),
    usb=()

# CORS headers for API endpoints if needed
# Uncomment and customize if you have API routes
# /api/**:
#   access-control-allow-origin: https://yourdomain.com
#   access-control-allow-methods: GET, POST, OPTIONS
#   access-control-allow-headers: Content-Type, Authorization
